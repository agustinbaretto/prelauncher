 <div class="row hero container-fluid">
     <div class="col-md-5 col-md-offset-1" style="padding-top: 70px;">
        <p class="small brandon">The creators of smart luggage are launching their next product</p>
     </div>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>